// theme.ts

export const COLORES = {
  // Colores principales de fondo
  fondo: '#1e272e',      // El negro azulado de fondo general
  tarjeta: '#2f3640',    // El gris oscuro de las tarjetas del ranking
  
  // Colores de acento y marca
  primario: '#ff4757',   // El rojo principal (botones, header, iconos)
  secundario: '#808e9b', // El gris clarito (para textos secundarios o botones inactivos)
  acento: '#0be881',     // El verde brillante (para el #1, éxitos y notificaciones)
  
  // Textos
  textoBlanco: '#ffffff',
  textoOscuro: '#000000',
};


export const FUENTES = {
  regular: 'FuentePrincipal',
  italic: 'FuentePrincipal-Italic',
};

// También podemos guardar espaciados globales por si la app crece
export const ESPACIADOS = {
  pequeno: 10,
  medio: 15,
  grande: 20,
  gigante: 30,
};